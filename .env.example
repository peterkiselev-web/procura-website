# Copy to .env and fill in. Never commit .env.
PORT=3000
HOST=127.0.0.1

# Optional: email each enquiry (requires `npm install nodemailer`)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ENQUIRY_TO=
ENQUIRY_FROM=
